import {
	Button as AriaButton,
	Group as AriaGroup,
	Separator as AriaSeparator,
	ToggleButton as AriaToggleButton,
	Toolbar as AriaToolbar,
} from "react-aria-components";
import { styled } from "restyle";

export const Button = styled(AriaButton);
export const Group = styled(AriaGroup);
export const Separator = styled(AriaSeparator);
export const ToggleButton = styled(AriaToggleButton);
export const Toolbar = styled(AriaToolbar);
